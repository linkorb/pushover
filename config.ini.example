; This config file stores test credentials for Pushover test-suite
;
; This is an example file. Copy / rename it to `config.ini` and add
; real values.
;
; Simply run the tests by entering `phpunit`
; 
; Retrieve your token by registering your app: 
; 	https://pushover.net/apps/build
;
; Retrieve your user_key here (login first!):
; 	https://pushover.net/
;

[pushover]
token = ""
user_key = ""
message = "Hello world :-)"

